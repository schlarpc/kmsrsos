[package]
name = "kmsrs-crypto"
description = "KMS protocol cryptographic primitives (ARCH-001)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sha2.workspace = true
hmac.workspace = true

[dev-dependencies]
# `alloc` for `hex::encode`, which the known-answer tests use so that a failure
# prints a hex string comparable by eye against the reference generator's output.
hex = { workspace = true, features = ["alloc"] }

[lints]
workspace = true
